apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-tour-app
  namespace: client-tour-test
  labels:
    app: client-tour
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-tour
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: client-tour
    spec:
      containers:
      - name: client-tour
        image: nginx:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "300m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 20
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: client-tour-app
  namespace: client-tour-test
spec:
  selector:
    app: client-tour
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
